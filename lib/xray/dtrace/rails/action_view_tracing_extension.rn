ActionView::Base.class_eval do
           
  def render_with_tracing(options = {}, old_local_assigns = {}, &block)
    XRay::DTrace::Tracer.firing('render', options.to_s) do
       render_without_tracing
    end
  end
      
  alias_method_chain :render, :tracing
          
end

